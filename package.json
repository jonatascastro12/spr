{
  "name": "gw",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "gw": "./src/cli.ts"
  },
  "scripts": {
    "build": "bun build src/cli.ts --compile --minify --bytecode --outfile dist/gw",
    "install-cli": "bun run build && cp dist/gw /usr/local/bin/gw && echo 'Installed gw to /usr/local/bin/gw'",
    "dev": "bun run src/cli.ts",
    "sync": "bun run src/cli.ts sync",
    "typecheck": "tsc --noEmit",
    "test:e2e": "bun test e2e",
    "test:e2e:ci": "bun run typecheck && bun test e2e"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}
